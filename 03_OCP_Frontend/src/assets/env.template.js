(function(window) {
   
    window.__env = window.__env || {};
     // // Environment variables
 
    window.API_GW_URL = "${API_GW_URL}"
    window.BACKEND_SC_API_URL = "${BACKEND_SC_API_URL}"

    
  })(this);